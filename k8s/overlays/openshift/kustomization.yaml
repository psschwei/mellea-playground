# OpenShift overlay for hosted deployment
# Deploys to a hosted OpenShift cluster with namespace-only access
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - storage/persistent-volume-claims.yaml
  - routes.yaml

# Optional patches:
# - hostname-patch.yaml  # Uncomment to set custom hostname for routes

# TODO: Add when needed:
# - image-pull-secret.yaml  # For private registry access
